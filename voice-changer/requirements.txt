# Web layer
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
httpx>=0.27

# Audio
soundfile>=0.12
numpy>=1.24,<2

# Local voice conversion (FreeVC / OpenVoice / kNN-VC).
# torchcodec is what torch>=2.9 uses for audio IO; it needs ffmpeg on the system.
coqui-tts>=0.27,<0.28
torch>=2.1
torchaudio>=2.1
torchcodec>=0.4
transformers>=4.40,<5
